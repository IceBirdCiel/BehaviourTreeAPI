using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameArcade : UsableObject
{
    private void Awake()
    {
        this.objectName = "game_arcade";
        this.timeToUse = 9f;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
