using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NaturalSeat : UsableObject
{
    private void Awake()
    {
        this.objectName = "natural_seat";
        this.timeToUse = 5f;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
